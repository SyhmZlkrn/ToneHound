# Install the runtime's CUDA-compatible torch first (see CLOUD_TRAINING.md).
# Direct dependencies, not a complete transitive lock; each run records versions.
torch>=2.6,<3
numpy==2.2.6
scipy==1.15.3
soundfile==0.14.0
transformers==5.16.1
huggingface-hub==1.30.0
safetensors==0.8.0
PyYAML==6.0.3
cmake>=3.24,<5
ninja>=1.11,<2
# Training uses a small tested LoRA linear layer; PEFT is not required.
